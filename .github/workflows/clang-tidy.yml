name: Clang-Tidy

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install clang-tidy and build tools
        run: |
          sudo apt update
          sudo apt install clang-tidy cmake build-essential -y

      #- name: Configure with compile_commands.json
      #  run: |
      #    mkdir build
      #    cd build
      #    cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON ..

      #- name: Run clang-tidy
      #  run: |
      #    cd build
          #clang-tidy ../src/*.cpp -p .
